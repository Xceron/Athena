<task>
    Your primary task is to categorize the provided whitepaper by selecting a set of tags that accurately describe its **core focus and main contributions**. Avoid tagging concepts that are merely mentioned or used as part of a larger methodology unless the concept itself is central to the paper's thesis.
</task>

<process>
    You must follow this two-step process:

    1.  **Reasoning**: First, analyze the paper's abstract, introduction, and conclusion to identify its primary subject matter and core contributions. Think step-by-step through the available tags. For each tag you select, write a brief justification for why it applies, explaining how it relates to the paper's main focus. For tags you considered but rejected (e.g., "MoE" for a paper that only uses an MoE model), briefly explain why it wasn't a primary focus. This entire process must be wrapped in `<reasoning>` tags.

    2.  **Tagging**: Second, after the reasoning block, provide the final list of chosen tags. This list must be wrapped in `<tags>` tags.
</process>

<limits>
    - Emit at most **four** tags.
    - Add a new tag only in the rare case that none of the existing tags can capture an essential, central topic of the paper.
    - Prefer the most general applicable tag; avoid synonyms or finer-grained variants.
</limits>

<definitions>
- CBR              <!-- Case-Based Reasoning -->
- CPS              <!-- Cyber-Physical Systems -->
- BPMN
- RL               <!-- Reinforcement Learning -->
- LLM              <!-- Large Language Models -->
- AI Planning      <!-- Symbolic planning: PDDL, HTN, Lean, etc. -->
- PDDL
- Search Algorithms<!-- MCTS, A*, Beam Search, etc. -->
- Model            <!-- A new model / model family is the main contribution -->
- Neurosymbolic AI
- Prompting
- Survey
- Benchmark        <!-- Mainly about a new benchmark -->
- Dataset          <!-- Mainly about a new dataset -->
- Case Study
- Hierarchical Planning
- LLM Agents
- Composite Systems
- MoE              <!-- Central focus on Mixture-of-Experts -->
- Ensemble
- Synthetic Data
- Verifier         <!-- Uses an external, sound verifier for LLM output -->
- LLM-as-judge     <!-- Uses an LLM to grade outputs -->
</definitions>

<formatting>
    - Start with the `<reasoning>` block.
    - Follow it immediately with the final `<tags>` block.
    - Inside `<tags>`, each tag must be on a new line and start with a hyphen (`-`).
    - Do not include the explanatory text from the definitions in the final output.
    - Do not add new tags, unless it is so important and general that it deserves ists own tag.
</formatting>
